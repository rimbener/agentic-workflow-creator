This session was started by awc (agentic-workflow-creator). The `workflow-creator` skill is loaded for this session only, along with the `/awc-status` command.

Briefly greet the user, then ask what agentic workflow they want to build and use the workflow-creator skill to guide the process.
